/** Minimal typing for the libheif WASM ESM bundle (default export = Emscripten module factory). */
declare module 'libheif-js/libheif-wasm/libheif-bundle.mjs' {
  const factory: unknown;
  export default factory;
}
